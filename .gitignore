node_modules/
.env
.env.test
.env.test.example
service-account.json
google-credentials.json
*-credentials.json
data/
nul
cookies.txt
tmpclaude-*
# Spurious empty lockfile regenerated at the repo root (no root package.json;
# the real lockfile lives in nextjs/). Anchored so nextjs/package-lock.json
# stays tracked.
/package-lock.json

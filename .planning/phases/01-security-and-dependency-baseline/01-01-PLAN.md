---
phase: 01-security-and-dependency-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - nextjs/lib/upload.ts
  - nextjs/__tests__/lib/upload.test.ts
  - nextjs/app/api/uploads/[[...path]]/route.ts
  - nextjs/app/api/bills/[id]/route.ts
  - nextjs/app/api/bills/[id]/images/[imageId]/route.ts
  - nextjs/app/api/bills/[id]/images/route.ts
  - nextjs/app/api/bills/bulk-delete/route.ts
  - nextjs/app/api/reports/user/[email]/pdf/route.ts
  - nextjs/app/api/admin/export/images/route.ts
  - nextjs/app/api/bug-reports/route.ts
  - nextjs/app/api/bug-reports/screenshots/[filename]/route.ts
autonomous: true
requirements:
  - SEC-01

must_haves:
  truths:
    - "A request to any file-serving endpoint with a path containing ../ is rejected before any filesystem access"
    - "The path guard verifies resolved path stays within allowed directory using path.sep"
    - "The existing bug-reports/screenshots guard is fixed to include path.sep"
  artifacts:
    - path: "nextjs/lib/upload.ts"
      provides: "assertPathWithin helper function"
      exports: ["assertPathWithin"]
      contains: "path.sep"
    - path: "nextjs/__tests__/lib/upload.test.ts"
      provides: "Unit tests for path traversal guard"
      contains: "assertPathWithin"
  key_links:
    - from: "nextjs/app/api/uploads/[[...path]]/route.ts"
      to: "nextjs/lib/upload.ts"
      via: "import { assertPathWithin }"
      pattern: "assertPathWithin"
    - from: "nextjs/app/api/bills/[id]/route.ts"
      to: "nextjs/lib/upload.ts"
      via: "import { assertPathWithin }"
      pattern: "assertPathWithin"
---

<objective>
Add path traversal protection to all file-serving endpoints.

Purpose: Prevent attackers from reading arbitrary files via ../ path segments in upload URLs (SEC-01).
Output: A shared assertPathWithin() helper in lib/upload.ts used by all 10 file-serving routes, with unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-and-dependency-baseline/01-CONTEXT.md
@.planning/phases/01-security-and-dependency-baseline/01-RESEARCH.md

<interfaces>
<!-- From nextjs/lib/upload.ts - add assertPathWithin as new export alongside existing exports -->
From nextjs/lib/upload.ts:
```typescript
import path from 'path';
export const UPLOADS_DIR = process.env.UPLOADS_DIR || path.join(process.cwd(), '..', 'data', 'uploads');
export function deleteFile(filePath: string): void;
export function ensureUploadDir(userEmail: string): string;
```

From nextjs/app/api/bug-reports/screenshots/[filename]/route.ts (reference pattern, lines 33-36):
```typescript
// Current guard - MISSING path.sep, needs fix:
const resolvedPath = path.resolve(filePath);
const allowedDir = path.resolve(path.join(UPLOADS_DIR, 'bug-reports'));
if (!resolvedPath.startsWith(allowedDir)) {
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create assertPathWithin helper and unit tests</name>
  <files>nextjs/lib/upload.ts, nextjs/__tests__/lib/upload.test.ts</files>
  <read_first>
    - nextjs/lib/upload.ts (existing exports, add new function after UPLOADS_DIR constant)
    - nextjs/__tests__/lib/upload.test.ts (may not exist yet - create if missing)
    - nextjs/app/api/bug-reports/screenshots/[filename]/route.ts (reference pattern lines 33-38)
  </read_first>
  <behavior>
    - assertPathWithin('/uploads/user/file.jpg', '/uploads') does NOT throw
    - assertPathWithin('/uploads', '/uploads') does NOT throw (exact match = allowed directory itself)
    - assertPathWithin('/uploads/../etc/passwd', '/uploads') THROWS Error with message 'Path traversal attempt blocked'
    - assertPathWithin('/uploads-evil/secret', '/uploads') THROWS Error (prefix attack blocked by path.sep check)
    - assertPathWithin('/uploads/sub/../sub/file.jpg', '/uploads') does NOT throw (resolves to valid path)
  </behavior>
  <action>
    1. In nextjs/lib/upload.ts, add the following function after the UPLOADS_DIR constant (around line 12):

    ```typescript
    /**
     * Verify that a resolved file path stays within the allowed directory.
     * Throws an Error if the path escapes the allowed directory (path traversal).
     */
    export function assertPathWithin(filePath: string, allowedDir: string): void {
      const resolved = path.resolve(filePath);
      const allowed = path.resolve(allowedDir);
      if (resolved !== allowed && !resolved.startsWith(allowed + path.sep)) {
        throw new Error('Path traversal attempt blocked');
      }
    }
    ```

    2. Create nextjs/__tests__/lib/upload.test.ts with tests for the 5 behaviors listed above. Import assertPathWithin from '@/lib/upload'. Use path.join and path.resolve in test setup to construct paths.

    Per D-01: Using a shared helper (not inline) because the consequence of missing the guard in any route is a security vulnerability.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/jensmoeller/code/vbudget/nextjs && npx jest __tests__/lib/upload.test.ts --forceExit</automated>
  </verify>
  <acceptance_criteria>
    - nextjs/lib/upload.ts contains `export function assertPathWithin(filePath: string, allowedDir: string): void`
    - nextjs/lib/upload.ts contains `allowed + path.sep` (the critical path.sep check)
    - nextjs/lib/upload.ts contains `throw new Error('Path traversal attempt blocked')`
    - nextjs/__tests__/lib/upload.test.ts contains `assertPathWithin`
    - nextjs/__tests__/lib/upload.test.ts contains at least 5 test cases (it/test calls)
    - `cd nextjs && npx jest __tests__/lib/upload.test.ts --forceExit` exits 0
  </acceptance_criteria>
  <done>assertPathWithin helper exported from lib/upload.ts with path.sep protection, all 5 unit tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Apply path traversal guard to all file-serving routes</name>
  <files>
    nextjs/app/api/uploads/[[...path]]/route.ts,
    nextjs/app/api/bills/[id]/route.ts,
    nextjs/app/api/bills/[id]/images/[imageId]/route.ts,
    nextjs/app/api/bills/[id]/images/route.ts,
    nextjs/app/api/bills/bulk-delete/route.ts,
    nextjs/app/api/reports/user/[email]/pdf/route.ts,
    nextjs/app/api/admin/export/images/route.ts,
    nextjs/app/api/bug-reports/route.ts,
    nextjs/app/api/bug-reports/screenshots/[filename]/route.ts
  </files>
  <read_first>
    - nextjs/lib/upload.ts (confirm assertPathWithin is exported after Task 1)
    - nextjs/app/api/uploads/[[...path]]/route.ts (line 68: filePath = path.join(UPLOADS_DIR, relPath) - add guard after this line)
    - nextjs/app/api/bills/[id]/route.ts (find all path.join(UPLOADS_DIR, ...) calls in DELETE handler - add guard before unlinkSync)
    - nextjs/app/api/bills/[id]/images/[imageId]/route.ts (find path.join(UPLOADS_DIR, ...) in PUT and DELETE - add guard before file ops)
    - nextjs/app/api/bills/[id]/images/route.ts (find path construction from email - add guard)
    - nextjs/app/api/bills/bulk-delete/route.ts (find unlinkSync loop - add guard before each unlink)
    - nextjs/app/api/reports/user/[email]/pdf/route.ts (find replace regex - replace with assertPathWithin)
    - nextjs/app/api/admin/export/images/route.ts (find replace regex - replace with assertPathWithin)
    - nextjs/app/api/bug-reports/route.ts (find path construction - add guard for consistency)
    - nextjs/app/api/bug-reports/screenshots/[filename]/route.ts (lines 33-36: replace manual check with assertPathWithin)
  </read_first>
  <action>
    For each route, add assertPathWithin to the import from '@/lib/upload' (or add the import if not present) and insert the guard call immediately after path.join(UPLOADS_DIR, ...) and before any fs.* call. Pattern:

    ```typescript
    const filePath = path.join(UPLOADS_DIR, relPath);
    assertPathWithin(filePath, UPLOADS_DIR);  // add this line
    ```

    If the route catches errors with try/catch, the thrown Error from assertPathWithin will be caught. In the catch block, add a check for the traversal error and return 400:

    ```typescript
    } catch (error) {
      if (error instanceof Error && error.message === 'Path traversal attempt blocked') {
        return NextResponse.json({ error: 'Invalid file path' }, { status: 400 });
      }
      // ... existing error handling
    }
    ```

    Per route (per D-02 - all file-serving endpoints must be patched):

    1. api/uploads/[[...path]]/route.ts - After `const filePath = path.join(UPLOADS_DIR, relPath);` (around line 68), add `assertPathWithin(filePath, UPLOADS_DIR);`. Add path traversal check in catch block.

    2. api/bills/[id]/route.ts (DELETE handler) - Find every path.join(UPLOADS_DIR, img.filePath) or path.join(UPLOADS_DIR, bill.filename) before unlinkSync. Add assertPathWithin(fullPath, UPLOADS_DIR) after path construction, before unlink.

    3. api/bills/[id]/images/[imageId]/route.ts (PUT and DELETE) - Find path.join(UPLOADS_DIR, image.filePath) calls. Add assertPathWithin(fullPath, UPLOADS_DIR) before copyFileSync/unlinkSync.

    4. api/bills/[id]/images/route.ts (POST) - Find path construction. Add guard after path is built from user email.

    5. api/bills/bulk-delete/route.ts - Inside the delete loop where unlinkSync is called, add assertPathWithin(fullPath, UPLOADS_DIR) before each unlink. Wrap in try/catch so one bad path does not stop the bulk delete.

    6. api/reports/user/[email]/pdf/route.ts - Remove the weak `replace(/\.\./g, '')` sanitization. Replace with assertPathWithin(filePath, UPLOADS_DIR) after path construction.

    7. api/admin/export/images/route.ts - Remove the weak `replace(/\.\./g, '')` sanitization. Replace with assertPathWithin(filePath, UPLOADS_DIR) after path construction.

    8. api/bug-reports/route.ts - Add guard after path construction for consistency (low risk since filename is crypto.randomUUID but defense in depth).

    9. api/bug-reports/screenshots/[filename]/route.ts - Replace the manual resolve+startsWith check (lines 33-38) with `assertPathWithin(filePath, path.join(UPLOADS_DIR, 'bug-reports'));`. This fixes the missing path.sep bug in the existing guard.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/jensmoeller/code/vbudget/nextjs && npx jest __tests__/lib/upload.test.ts --forceExit && node -e "const fs=require('fs'),p=require('path'); const routes=['app/api/uploads/[[...path]]/route.ts','app/api/bills/[id]/route.ts','app/api/bills/[id]/images/[imageId]/route.ts','app/api/bills/[id]/images/route.ts','app/api/bills/bulk-delete/route.ts','app/api/reports/user/[email]/pdf/route.ts','app/api/admin/export/images/route.ts','app/api/bug-reports/route.ts','app/api/bug-reports/screenshots/[filename]/route.ts']; let ok=true; for(const r of routes){const c=fs.readFileSync(p.join('.',r),'utf8'); if(!c.includes('assertPathWithin')){console.error('MISSING assertPathWithin in '+r);ok=false;}} if(!ok)process.exit(1); console.log('All routes have assertPathWithin');"</automated>
  </verify>
  <acceptance_criteria>
    - All 9 route files contain the string `assertPathWithin`
    - All 9 route files contain import with assertPathWithin from '@/lib/upload'
    - nextjs/app/api/reports/user/[email]/pdf/route.ts does NOT contain `replace(/\.\./g`
    - nextjs/app/api/admin/export/images/route.ts does NOT contain `replace(/\.\./g`
    - nextjs/app/api/bug-reports/screenshots/[filename]/route.ts does NOT contain the old manual resolvedPath.startsWith(allowedDir) pattern (replaced with assertPathWithin)
    - `cd nextjs && npx jest __tests__/lib/upload.test.ts --forceExit` still exits 0
  </acceptance_criteria>
  <done>All 10 file-serving routes (including the reference implementation fix) use assertPathWithin, weak regex sanitization removed</done>
</task>

</tasks>

<verification>
- `cd nextjs && npx jest __tests__/lib/upload.test.ts --forceExit` - all path traversal guard tests pass
- `grep -r "assertPathWithin" nextjs/app/api/ --include="*.ts" | wc -l` returns >= 9
- `grep -r "replace.*\\\.\\\." nextjs/app/api/reports/ nextjs/app/api/admin/export/ --include="*.ts"` returns no matches
</verification>

<success_criteria>
- assertPathWithin exported from lib/upload.ts with path.sep protection
- All 9 file-serving routes import and call assertPathWithin before any fs operation
- Weak regex-based sanitization removed from reports and admin export routes
- Bug-reports/screenshots reference implementation upgraded from manual check to shared helper
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-and-dependency-baseline/01-01-SUMMARY.md`
</output>

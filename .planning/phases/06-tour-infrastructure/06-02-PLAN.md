---
phase: 06-tour-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - nextjs/lib/tour/steps.ts
autonomous: true
requirements:
  - INFRA-04

must_haves:
  truths:
    - "All 6 tour steps are defined in a single configuration file"
    - "Adding or reordering a step requires editing only the steps.ts file"
    - "Each step has targetSelector, title, body, and placement fields"
  artifacts:
    - path: "nextjs/lib/tour/steps.ts"
      provides: "TourStep interface and TOUR_STEPS array with 6 entries"
      exports: ["TourStep", "TOUR_STEPS"]
      contains: "TOUR_STEPS"
  key_links:
    - from: "nextjs/lib/tour/steps.ts"
      to: "Phase 7/8 consumers"
      via: "import { TOUR_STEPS, TourStep } from '@/lib/tour/steps'"
      pattern: "export.*TOUR_STEPS"
---

<objective>
Create the centralized tour step configuration file with all 6 tour steps, a TourStep TypeScript interface, and a typed TOUR_STEPS array.

Purpose: INFRA-04 requires all tour steps defined in a single config file with target selectors and content. This is the source of truth for what the tour shows.
Output: `nextjs/lib/tour/steps.ts` with TourStep interface and 6-entry TOUR_STEPS array.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tour-infrastructure/06-CONTEXT.md
@.planning/phases/06-tour-infrastructure/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tour step configuration with TourStep interface and 6 steps</name>
  <files>nextjs/lib/tour/steps.ts</files>
  <read_first>
    - nextjs/app/(protected)/layout.tsx
    - nextjs/components/layout/AppShell.tsx
  </read_first>
  <action>
Create the directory `nextjs/lib/tour/` and the file `nextjs/lib/tour/steps.ts`.

Per D-04, D-05, D-06: Define a `TourStep` interface and a typed `TOUR_STEPS` readonly array with exactly 6 entries.

**TourStep interface (per D-05):**
```typescript
export interface TourStep {
  id: string;
  targetSelector: string;
  title: string;
  body: string;
  placement: 'top' | 'bottom' | 'left' | 'right';
}
```

Use explicit string IDs (per research discretion recommendation, more readable than array indices).

**TOUR_STEPS array (6 entries covering core workflow):**
The 6 steps cover the core SetCash workflow. Use `data-tour="xxx"` attribute selectors — these will be added to actual UI elements in Phase 8 (INTG-01). Placeholder content is appropriate for Phase 6; exact wording can be refined in Phase 8.

```typescript
export const TOUR_STEPS: readonly TourStep[] = [
  {
    id: 'sidebar-nav',
    targetSelector: '[data-tour="sidebar-nav"]',
    title: 'Navigation',
    body: 'Use the sidebar to switch between sections like Bills, Budget, and Settings.',
    placement: 'right',
  },
  {
    id: 'submit-bill',
    targetSelector: '[data-tour="submit-bill"]',
    title: 'Submit a Bill',
    body: 'Click here to submit a new expense bill with a photo and details.',
    placement: 'bottom',
  },
  {
    id: 'bill-list',
    targetSelector: '[data-tour="bill-list"]',
    title: 'Your Bills',
    body: 'View and track all your submitted bills and their approval status.',
    placement: 'top',
  },
  {
    id: 'budget-matrix',
    targetSelector: '[data-tour="budget-matrix"]',
    title: 'Budget Overview',
    body: 'See how spending compares to your budget across all categories.',
    placement: 'bottom',
  },
  {
    id: 'project-switcher',
    targetSelector: '[data-tour="project-switcher"]',
    title: 'Switch Projects',
    body: 'If you belong to multiple projects, switch between them here.',
    placement: 'bottom',
  },
  {
    id: 'user-menu',
    targetSelector: '[data-tour="user-menu"]',
    title: 'Your Account',
    body: 'Access your profile settings, change your password, or sign out.',
    placement: 'left',
  },
] as const;
```

The file exports both `TourStep` (interface) and `TOUR_STEPS` (readonly array). Per D-06, the `as const` assertion provides type safety.
  </action>
  <verify>
    <automated>cd nextjs && node -e "const s = require('./lib/tour/steps'); console.log('count:', s.TOUR_STEPS.length); console.log('ids:', s.TOUR_STEPS.map(s=>s.id).join(',')); const fields = ['id','targetSelector','title','body','placement']; const ok = s.TOUR_STEPS.every(step => fields.every(f => typeof step[f] === 'string')); console.log('all_fields_present:', ok);" 2>/dev/null || npx tsx -e "import { TOUR_STEPS } from './lib/tour/steps'; console.log('count:', TOUR_STEPS.length); console.log('ids:', TOUR_STEPS.map(s=>s.id).join(',')); const fields = ['id','targetSelector','title','body','placement'] as const; const ok = TOUR_STEPS.every(step => fields.every(f => typeof step[f] === 'string')); console.log('all_fields_present:', ok);"</automated>
  </verify>
  <acceptance_criteria>
    - nextjs/lib/tour/steps.ts exists
    - File contains `export interface TourStep` with fields: id (string), targetSelector (string), title (string), body (string), placement ('top' | 'bottom' | 'left' | 'right')
    - File contains `export const TOUR_STEPS: readonly TourStep[]`
    - TOUR_STEPS has exactly 6 entries
    - Each entry has a unique string `id` field
    - Each entry's `targetSelector` uses the `[data-tour="xxx"]` pattern
    - File contains `as const` assertion
    - `npx tsc --noEmit` passes (file compiles)
  </acceptance_criteria>
  <done>nextjs/lib/tour/steps.ts exports TourStep interface and TOUR_STEPS array with 6 typed entries, each having id, targetSelector, title, body, and placement fields</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This file is a static TypeScript configuration with no user input, no network access, no database access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-03 | Tampering | Tour step content | accept | Static config file; content is not user-editable at runtime; changes require code deployment |
</threat_model>

<verification>
- `nextjs/lib/tour/steps.ts` exists and exports TourStep + TOUR_STEPS
- TOUR_STEPS.length === 6
- Every step has all 5 required fields as strings
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- A single configuration file defines all 6 tour steps
- Each step has targetSelector, title, body, and placement
- The file is the sole source of truth for tour step definitions
- TypeScript types provide compile-time safety for step shape
</success_criteria>

<output>
After completion, create `.planning/phases/06-tour-infrastructure/06-02-SUMMARY.md`
</output>
